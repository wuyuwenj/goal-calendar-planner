# Trellis - Goal Planner Mobile App

A React Native mobile app that helps you achieve your goals through AI-powered planning and progressive task scheduling.

## Features

- **AI-Powered Goal Planning**: Set a goal and get a personalized weekly plan generated by AI
- **Progressive Difficulty**: Plans automatically scale from easy (foundation phase) to challenging (peak phase)
- **Task Tracking**: Check off tasks as you complete them with satisfying animations
- **Task Details**: Tap any task to see explanations, resources, and helpful links
- **Weekly Check-ins**: Review your progress and get adjusted plans based on performance
- **Google Calendar Sync**: Sync your tasks to Google Calendar for reminders
- **Beautiful Design**: Trellis design system with calming green and brown earth tones

## Screenshots

The app features a clean, nature-inspired design:
- Dashboard with current goal progress and weekly tasks
- Calendar view showing all scheduled tasks
- Task detail modal with resources and explanations
- Settings with profile and calendar sync options

## Tech Stack

- **Framework**: React Native with Expo
- **Navigation**: Expo Router (file-based routing)
- **State Management**: Zustand
- **Authentication**: Supabase Auth with Google OAuth
- **Styling**: Custom design system (Trellis)
- **Icons**: Lucide React Native

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Copy environment variables:
   ```bash
   cp .env.example .env
   ```

3. Configure `.env`:
   ```
   EXPO_PUBLIC_API_URL=http://your-backend-url:3000/api
   EXPO_PUBLIC_SUPABASE_URL=your-supabase-url
   EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
   ```

4. Start the development server:
   ```bash
   npx expo start
   ```

5. Run on device/simulator:
   - Press `i` for iOS simulator
   - Press `a` for Android emulator
   - Scan QR code with Expo Go app for physical device

## Project Structure

```
app/
├── (auth)/           # Authentication screens
│   └── login.tsx
├── (tabs)/           # Main tab navigation
│   ├── index.tsx     # Dashboard
│   ├── calendar.tsx  # Calendar view
│   └── settings.tsx  # Settings
└── onboarding/       # Goal creation flow

components/
├── ui/               # Reusable UI components
├── GoalCard.tsx      # Goal progress card
├── TaskItem.tsx      # Task list item
└── TaskDetailModal.tsx # Task details popup

lib/
├── api.ts            # API client with auth
├── supabase.ts       # Supabase client
└── notifications.ts  # Push notifications

store/
├── auth.ts           # Auth state (Zustand)
└── goal.ts           # Goal/task state (Zustand)

constants/
└── theme.ts          # Trellis design system colors
```

## Design System (Trellis)

The app uses a nature-inspired color palette:

- **Primary**: Forest green (`#2D5A3D`) - growth, progress
- **Secondary**: Warm brown (`#8B7355`) - earth, stability
- **Accent**: Sage green (`#7BA seventeen`) - calm, balance
- **Background**: Cream (`#FAF8F5`) - natural, clean

## Backend

This app requires the [Trellis Backend](./goal-planner-backend) to be running. See the backend README for setup instructions.

## License

MIT
